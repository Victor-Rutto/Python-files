numbers = [1, 2, 3, 4, 5]
print(numbers)
numbers.append(6)
print(numbers)
numbers.insert(0, -1)   # Only inserts one character
print(numbers)
numbers.remove(-1)  # Only removes one character
print(numbers)
#numbers.clear()     # Removes all characters
print(1 in numbers)  # Check whether 1 is in the list of numbers
#   Built-in lin function to count the number of characters
print(len(numbers))
