# LIST (SEQUENCE DATA TYPES)
# Allows storage of different data types
# Dinamic list data types
# Items have order/ordered
# List are mutable
# Stored in squared brackets

numbers = [10, 1, 3, 5]
names = ['victor', 'monica']
mix_list = [10, 'victor', 10.5]

# Indexing
print(numbers[3])

# length
print(len(numbers))

# slicing
print(numbers[:-1])
numbers.append(45)
print(numbers)


